## Refactor 項目

UI 層畫面、工具列、編輯器、快捷鍵
盡量共用

資料層電路資料結構
選取狀態
歷史復原 redo/undo
盡量共用

儲存層server storage
local storage
IndexedDB storage
這層才分岔

模板/LaTeX 層template 讀取
latex proxy
這層也可以做成可替換模組

如果做到這樣，以後切換 demo / 正式版就只是改設定，不是重寫整個專案。

main正式版持續開發
demo/b-local-storage-vercel只做方案 B 需要的改動

src/config/ 或類似位置放一個 appMode.ts
決定目前走 server 還是 local

demo 的改動盡量可回退不要一次把 server.js 大改掉
最好保留舊後端程式，只是讓 demo 版不再走它
也就是「替換入口」，不是「刪除舊功能」

推薦的實作方式
在程式內加一個切換層，例如：storageMode = "server" 或 "local"

demo 版預設 local
正式版預設 server
這樣未來只要改一個設定，就能切換
